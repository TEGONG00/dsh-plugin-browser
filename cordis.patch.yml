# dsh-plugin-browser dev overlay — load via:
#   npx @deepseek-ai/dsh web --patch ./dsh-plugin-browser/cordis.patch.yml
# The name must be an absolute path to the built host entry.
- insert:
  - id: browser
    name: /home/naxnax/deepseek-project/one-project/dsh-plugin-browser/lib/index.js
    config:
      headless: true
      viewport:
        width: 1280
        height: 800
      jpegQuality: 60
